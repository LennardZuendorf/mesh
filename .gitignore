# --- Secrets & local config (never commit) ---
.mesh/
config.toml
config.local.toml
.env
.env.*
*.secret

# --- Mesh CLI derived cache / search index (disposable) ---
.mesh-index/
*.mesh-index
index.sqlite
index.sqlite-*

# --- Rust ---
/target/
**/*.rs.bk

# --- Testing / tooling ---
llvm-cov/
*.profraw

# --- Agent tooling caches ---
.serena/

# --- Editors / OS ---
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# subagent-driven-development scratch workspace (ledger, briefs, review packages)
.superpowers/

# transient git worktrees created for isolated agent runs
.claude/worktrees/
